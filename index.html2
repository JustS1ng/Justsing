<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>JustSing - Totebags</title>
  <link href="https://fonts.googleapis.com/css2?family=Comfortaa&display=swap" rel="stylesheet">
  <style>
        button:hover {
      background-color: #6d0420;
    }

    footer {
      text-align: center;
      padding: 1.5em;
      background-color: #eee;
      margin-top: 2em;
    }

    .cart-btn {
      position: fixed;
      top: 1.2em;
      right: 1.2em;
      background-color: transparent;
      border: none;
      cursor: pointer;
      z-index: 9999;
    }

    .cart-btn img {
      width: 50px;
      height: 50px;
    }

    .carrito {
      display: none;
      position: fixed;
      right: 0;
      top: 0;
      width: 300px;
      height: 100vh;
      background-color: #fff6f0;
      box-shadow: -2px 0 10px rgba(0,0,0,0.2);
      padding: 1em;
      overflow-y: auto;
      z-index: 1000;
    }

    .carrito h2 {
      margin-top: 0;
      color: #890620;
    }

    .carrito button {
      margin-left: 0.5em;
      background-color: #d94c3a;
    }

    @media (max-width: 600px) {
      .productos {
        grid-template-columns: 1fr;
      }
      .carrito {
        width: 100%;
      }

    /* === ESTILOS NUEVOS QUE DEBES AGREGAR === */
    /* Botón del menú ☰ */
    .menu-btn {
      position: fixed;
      top: 20px;
      left: 20px;
      z-index: 1001;
      background: #890620;
      color: white;
      border: none;
      padding: 10px 15px;
      font-size: 1.2rem;
      cursor: pointer;
      border-radius: 5px;
      display: none; /* Oculta en desktop por defecto */
    }

    /* Sidebar (menú lateral) */
    .sidebar {
      position: fixed;
      top: 0;
      left: -250px; /* Oculta fuera de la pantalla */
      width: 250px;
      height: 100vh;
      background: #890620;
      color: white;
      transition: left 0.3s ease;
      z-index: 1000;
      padding-top: 60px;
    }

    /* Clase para mostrar el sidebar (se activa con JavaScript) */
    .sidebar.open {
      left: 0;
    }

    /* Estilos de los enlaces del menú */
    .sidebar ul {
      list-style: none;
      padding: 0;
    }

    .sidebar li a {
      color: white;
      text-decoration: none;
      display: block;
      padding: 15px;
      transition: background 0.3s;
    }

    .sidebar li a:hover {
      background: #6d0420;
    }

    /* Mostrar botón solo en móvil/tablet */
    @media (max-width: 768px) {
      .menu-btn {
        display: block;
      }
    }
  </style>
</head>
